(()=>{
const $=id=>document.getElementById(id),KEY='musicTeacherTermLearningV1',PM='musicTeacherTermPointsModeV1';const load=()=>{try{return JSON.parse(localStorage.getItem(KEY))||{}}catch{return{}}};function current(){const n=$('termName')?.textContent?.trim();return (window.TERM_PROMPTS||[]).find(x=>x.term===n)}function ready(s){const a=+s?.attempts||0,p=+s?.passes||0,b=+s?.best||0,rate=a?p/a:0;return a>=3&&p>=2&&rate>=.67&&b>=75}function action(r){if(!r||(!r.recognition&&!r.expression&&!r.at))return{icon:'👀',title:'先做第一次無提示回想',text:'不要先看中文答案。先判斷熟悉度，再寫出中文名稱與一句核心定義。'};if((+r.recognition||0)<60)return{icon:'🧠',title:'先補辨識力',text:'看到原文後，練到能立即說出中文名稱、時期或最基本概念。'};if((+r.expression||0)<60)return{icon:'✍️',title:'現在最需要補表達力',text:'用自己的話寫「一句定義＋兩個鑑別點」，不要只重讀標準答案。'};if(!r.mastery)return{icon:'📅',title:'進入跨天回想',text:r.due&&new Date(r.due)<=new Date()?'這題已到期，現在適合無提示再寫一次。':'今天先停在這裡，等系統到期後再回想，才能確認不是短期記憶。'};const m=r.examModes||{};if(!ready(m.three)){localStorage.setItem(PM,'3');return{icon:'🎯',title:'知識會了，改練 3 分輸出',text:'把答案壓縮成「準確定義＋最有鑑別力的一點」，練短而準。'}}if(!ready(m.five)){localStorage.setItem(PM,'5');return{icon:'🎯',title:'3 分已穩，挑戰 5 分輸出',text:'補上背景／核心特徵與人物、作品或例證，練完整但不冗長。'}}if((+r.teaching||0)<60)return{icon:'👩‍🏫',title:'最後補教學力',text:'想像面對國中生：你會用什麼聽覺例子、遊戲、比較或任務讓學生理解這個名詞？'};return{icon:'🏆',title:'這題已形成完整能力',text:'知識、跨天記憶、3／5 分輸出與教學轉化都已有基礎；之後只需依間隔複習再驗證。'}}function render(){const view=$('termView'),item=current();if(!view||!item)return;let box=$('termNextAction');if(!box){box=document.createElement('div');box.id='termNextAction';box.className='score-guide';const anchor=$('termMasteryLadder')||$('termScoreGuide');anchor?.insertAdjacentElement('afterend',box)}if(!box)return;const a=action(load()[item.id]);box.innerHTML=`<b>${a.icon} 下一步只做這件事</b><div style="margin-top:5px"><strong>${a.title}</strong></div><small>${a.text}</small>`}function init(){render();setInterval(render,1400);document.addEventListener('termExamModeUpdated',render)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,500));else setTimeout(init,500);
})();