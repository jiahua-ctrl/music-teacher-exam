(()=>{if(window.__MUSIC_TEACHER_SRS_V5__)return;window.__MUSIC_TEACHER_SRS_V5__=true;const REVIEW_KEY='musicTeacherExamSpacedReviewV1',STATS_KEY='musicTeacherExamStatsV1';const load=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))||f}catch{return f}},save=(k,v)=>localStorage.setItem(k,JSON.stringify(v)),questions=()=>Array.isArray(window.LOCAL_QUESTIONS)?window.LOCAL_QUESTIONS:[],stats=()=>{const s=load(STATS_KEY,{});return{attempts:s.attempts||{},wrong:Array.isArray(s.wrong)?s.wrong:[],unknown:Array.isArray(s.unknown)?s.unknown:[],daily:s.daily||{},topics:s.topics||{}}},addHours=(d,h)=>new Date(d.getTime()+h*3600000),addDays=(d,x)=>addHours(d,x*24),fmt=d=>d?`${d.getMonth()+1}/${d.getDate()}`:'—',intervalFor=n=>n<=1?3:n===2?7:n===3?14:n===4?30:n===5?60:90,idSet=a=>new Set((a||[]).map(String));
function bootstrap(){const r=load(REVIEW_KEY,{}),s=stats(),unknown=idSet(s.unknown);let changed=false;questions().forEach(q=>{const a=s.attempts?.[q.id],id=String(q.id);if(!a?.total)return;if(!r[q.id]){const acc=(a.correct||0)/a.total,isUnknown=unknown.has(id)||(a.unknown||0)>0;r[q.id]={due:new Date().toISOString(),correctStreak:isUnknown?0:(acc===1?1:0),lastResult:isUnknown?'unknown':(acc===1?'correct':'wrong'),hadWrong:isUnknown||acc<1,lastReviewed:null,intervalDays:0};changed=true}});if(changed)save(REVIEW_KEY,r);return r}
function scheduleFocus(q){if(!q?.id)return null;const r=bootstrap(),now=new Date(),prev=r[q.id]||{};if(prev.noteFocus)return new Date(prev.due||now);r[q.id]={...prev,noteFocus:true,noteFocusAddedAt:now.toISOString(),noteFocusPreviousDue:prev.due||null,noteFocusPreviousIntervalDays:prev.intervalDays??null,due:now.toISOString(),correctStreak:prev.correctStreak||0,hadWrong:prev.hadWrong||false,lastResult:prev.lastResult||'focus',lastReviewed:prev.lastReviewed||null,intervalDays:0};save(REVIEW_KEY,r);render();return now}
function unscheduleFocus(q){if(!q?.id)return;const r=load(REVIEW_KEY,{}),x=r[q.id];if(!x?.noteFocus)return;const s=stats(),hasAttempt=!!s.attempts?.[q.id]?.total,restored=x.noteFocusPreviousDue,clean={...x,noteFocus:false};delete clean.noteFocusAddedAt;delete clean.noteFocusPreviousDue;delete clean.noteFocusPreviousIntervalDays;if(restored){clean.due=restored;if(x.noteFocusPreviousIntervalDays!=null)clean.intervalDays=x.noteFocusPreviousIntervalDays;r[q.id]=clean}else if(hasAttempt)r[q.id]=clean;else delete r[q.id];save(REVIEW_KEY,r);render()}
function effectiveRisk(q,r,s){const x=r[q.id]||{},id=String(q.id),streak=x.correctStreak||0,unknown=idSet(s.unknown),wrong=idSet(s.wrong);if(x.lastResult==='unknown')return'unknown';if(x.lastResult==='wrong')return'wrong';if(streak>=2)return null;if(unknown.has(id))return'unknown';if(wrong.has(id))return'wrong';return null}
function stateOf(q,r,s,now=new Date()){const a=s.attempts?.[q.id],x=r[q.id];if(!a?.total&&!x?.noteFocus)return'new';if(effectiveRisk(q,r,s))return'red';if((x?.correctStreak||0)>=3&&new Date(x.due)>now)return'green';return'yellow'}
function summary(){const r=bootstrap(),s=stats(),now=new Date(),out={green:0,yellow:0,red:0,new:0,due:[],next:null,seen:0};questions().forEach(q=>{const st=stateOf(q,r,s,now);out[st]++;if(st!=='new')out.seen++;const x=r[q.id];if(x?.due){const d=new Date(x.due);if(d<=now)out.due.push({q,state:st,due:d,noteFocus:!!x.noteFocus});else if(!out.next||d<out.next)out.next=d}});out.due.sort((a,b)=>({red:0,yellow:1,green:2,new:3}[a.state]-({red:0,yellow:1,green:2,new:3}[b.state])||Number(b.noteFocus)-Number(a.noteFocus)||a.due-b.due);return out}
function update(q,correct,status='answer'){const r=bootstrap(),prev=r[q.id]||{correctStreak:0,hadWrong:false,lastResult:null},now=new Date(),keepFocus=!!prev.noteFocus;let due;if(status==='unknown'){due=addDays(now,1);r[q.id]={...prev,due:due.toISOString(),correctStreak:0,hadWrong:true,lastResult:'unknown',lastReviewed:now.toISOString(),intervalDays:1,noteFocus:keepFocus}}else if(!correct){const repeated=prev.lastResult==='wrong'||prev.lastResult==='unknown';due=repeated?addDays(now,1):addHours(now,4);r[q.id]={...prev,due:due.toISOString(),correctStreak:0,hadWrong:true,lastResult:'wrong',lastReviewed:now.toISOString(),intervalDays:repeated?1:0,noteFocus:keepFocus}}else{const n=(prev.correctStreak||0)+1,days=intervalFor(n);due=addDays(now,days);r[q.id]={...prev,due:due.toISOString(),correctStreak:n,hadWrong:n>=2?false:!!prev.hadWrong,lastResult:'correct',lastReviewed:now.toISOString(),intervalDays:days,noteFocus:keepFocus,graduatedFromRisk:n>=2?now.toISOString():prev.graduatedFromRisk||null}}if(keepFocus){r[q.id].noteFocusPreviousDue=r[q.id].due;r[q.id].noteFocusPreviousIntervalDays=r[q.id].intervalDays}else{delete r[q.id].noteFocusAddedAt;delete r[q.id].noteFocusPreviousDue;delete r[q.id].noteFocusPreviousIntervalDays}save(REVIEW_KEY,r);return due}
function phase(sum){const total=Math.max(1,questions().length),coverage=sum.seen/total;if(coverage<.4)return{name:'實力累積期',text:'先建立主要考點的理解與題感；新內容讀完立刻做題。'};if(coverage<.8)return{name:'記憶複習期',text:'開始把看懂的內容轉成能主動提取的記憶，增加到期複習與錯題回收。'};return{name:'大量做題期',text:'題庫覆蓋已高，改以混合題、限時作答與輸出練習檢查能不能用得出來。'}}
function ensureStyles(){if(document.getElementById('mtSrsStyles'))return;const x=document.createElement('style');x.id='mtSrsStyles';x.textContent='.mt-srs{padding:24px;margin-bottom:18px}.mt-srs-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.mt-phase{padding:7px 10px;border-radius:999px;background:var(--soft);color:var(--brand);font-size:12px;font-weight:800}.mt-srs-grid{display:grid;grid-template-columns:1.05fr .95fr;gap:16px;margin-top:15px}.mt-due{padding:18px;border-radius:16px;background:var(--soft)}.mt-due strong{font-size:42px;color:var(--brand)}.mt-memory{display:grid;grid-template-columns:1fr 1fr;gap:9px}.mt-state{padding:12px;border:1px solid var(--line);border-radius:12px}.mt-state b{display:block;font-size:22px}.mt-state small{color:var(--muted)}.mt-review-btn{display:inline-block;text-decoration:none;padding:10px 13px;border-radius:11px;background:var(--brand);color:#fff;font-weight:800}.mt-srs-foot{margin-top:13px;color:var(--muted);font-size:13px;line-height:1.6}.mt-next-review{margin:10px 0;padding:9px 11px;border-radius:10px;background:var(--soft);font-size:13px;font-weight:700}@media(max-width:720px){.mt-srs-grid{grid-template-columns:1fr}}';document.head.appendChild(x)}
function render(){const home=document.getElementById('homeView');if(!home)return;let p=document.getElementById('mtSrs');if(!p){p=document.createElement('section');p.id='mtSrs';p.className='card mt-srs';const anchor=document.getElementById('mtCoach')||document.querySelector('.section-card');anchor?.insertAdjacentElement('afterend',p)}const s=summary(),ph=phase(s),due=s.due.length;p.innerHTML=`<div class="mt-srs-head"><div><span class="eyebrow">間隔複習 3→7→14→30→60→90 天</span><h3 style="margin:4px 0 0">🧠 記憶狀態</h3></div><span class="mt-phase">${ph.name}</span></div><div class="mt-srs-grid"><div class="mt-due"><small>今日到期複習</small><div><strong>${due}</strong> 題</div><p>${due?'真實作答結果決定複習時間；歷史錯題或「不知道」在連續答對 2 次後會解除高風險。':`今天沒有到期題目。${s.next?'下一批約 '+fmt(s.next)+'。':'先做新題建立第一批複習排程。'}`}</p>${due?'<a class="mt-review-btn" href="review.html">🧠 開始今日到期複習</a>':''}</div><div class="mt-memory"><div class="mt-state"><small>🟢 已掌握（連對 3+）</small><b>${s.green}</b></div><div class="mt-state"><small>🟡 已解除風險／待複習</small><b>${s.yellow}</b></div><div class="mt-state"><small>🔴 目前仍不穩</small><b>${s.red}</b></div><div class="mt-state"><small>🆕 尚未練習</small><b>${s.new}</b></div></div></div><div class="mt-srs-foot"><b>${ph.name}</b>｜${ph.text}<br>熟練規則：最新一次答錯或不知道 → 🔴；之後連續答對 2 次 → 🟡解除高風險；連續答對 3 次 → 🟢已掌握。歷史紀錄會保留，但不再永久綁住目前熟練度。</div>`;window.HomeCategoryOrganizer?.apply?.()}
function observe(){const fb=document.getElementById('feedback');if(!fb||fb.dataset.srsObserved)return;fb.dataset.srsObserved='1';new MutationObserver(()=>{if(fb.classList.contains('hidden')){fb.dataset.srs='';return}const title=document.getElementById('feedbackTitle')?.textContent||'',isUnknown=title.includes('我不知道'),correct=title.includes('答對'),wrong=title.includes('正確答案');if(!correct&&!wrong&&!isUnknown)return;const text=document.getElementById('questionText')?.textContent||'',q=questions().find(x=>x.question===text);if(!q)return;const a=stats().attempts?.[q.id]?.total||0,key=`${q.id}:${a}`;if(fb.dataset.srs===key)return;fb.dataset.srs=key;const due=update(q,correct,isUnknown?'unknown':'answer');let note=document.getElementById('mtNextReview');if(!note){note=document.createElement('div');note.id='mtNextReview';note.className='mt-next-review';document.getElementById('sourceBox')?.before(note)}const x=load(REVIEW_KEY,{})[q.id],streak=x?.correctStreak||0;note.textContent=isUnknown?`❓ 已排入明日優先複習：${fmt(due)}。`:correct?(streak>=3?`🟢 已連續答對 ${streak} 次，進入已掌握；下一次複習 ${fmt(due)}。`:streak===2?`🟡 已連續答對 2 次，解除舊錯題／不知道高風險；下一次複習 ${fmt(due)}。`:`🧠 第 1 次答對，下一次複習 ${fmt(due)}。`):`🔴 重新進入高風險，會在 ${fmt(due)} 再安排一次。`;setTimeout(render,0)}).observe(fb,{attributes:true,attributeFilter:['class']})}
function integrateTermLearning(){const btn=document.getElementById('termBtn');if(!btn||btn.dataset.srsTermLinked)return;btn.dataset.srsTermLinked='1';btn.textContent='🧠 開始音樂名詞主動提取';btn.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();location.href='term-prototype-v3.html'},true)}
function start(){ensureStyles();bootstrap();integrateTermLearning();setTimeout(()=>{render();observe()},30)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();window.MusicTeacherSRS={summary,update,bootstrap,stateOf,effectiveRisk,intervalFor,render,scheduleFocus,unscheduleFocus};})();