(()=>{
const KEY='musicTeacherTermLearningV1';
const $=id=>document.getElementById(id);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const load=()=>{try{return JSON.parse(localStorage.getItem(KEY))||{}}catch{return{}}};
const save=x=>localStorage.setItem(KEY,JSON.stringify(x));
const norm=s=>String(s||'').toLowerCase().replace(/[\s\-–—／/、，。；：:()（）《》〈〉「」『』]/g,'');
const tokens=s=>String(s||'').split(/[；;、，,／/｜|\n]/).map(x=>x.trim()).filter(x=>x.length>=2);
const zhAnswers=t=>String(t.zh||'').split(/[／/、；;]/).map(x=>x.trim()).filter(Boolean);
const contains=(text,word)=>norm(text).includes(norm(word));
const dayKey=d=>{const x=new Date(d);return `${x.getFullYear()}-${String(x.getMonth()+1).padStart(2,'0')}-${String(x.getDate()).padStart(2,'0')}`};
const intervals=[1,3,7,14,30,60,90];
function inferDims(t){const text=`${t.definition||''} ${t.exam||''} ${t.related||''}`,dims=[];const rules=[['🎵 What｜核心定義',tokens(t.definition).slice(0,4)],['🕰️ When｜時代／年代',(text.match(/(?:\d{2,4}世紀|\d{3,4}年代|中世紀|文藝復興|巴洛克|古典時期|浪漫時期|20世紀|現代|當代)/g)||[])],['🌍 Where｜地點／國家',(text.match(/(?:法國|德國|義大利|美國|日本|韓國|印度|葡萄牙|西非|中亞|威尼斯|巴黎|維也納|聖馬可大教堂|蒙古|圖瓦)/g)||[])],['👤 Who｜人物',tokens(t.related).filter(x=>/[A-Za-zÀ-ž]{3,}/.test(x)||/[·・]/.test(x)).slice(0,4)],['🎼 Works／例證',tokens(t.related).filter(x=>/《|Sonata|Passion|Prometheus|Birth|Pierrot|Symphonie|Wozzeck|opera|concerto/i.test(x)).slice(0,3)]];rules.forEach(([label,ks])=>{ks=[...new Set(ks)].filter(Boolean);if(ks.length)dims.push([label,ks])});if(t.exam)dims.push(['🎯 Exam｜教甄高鑑別度',tokens(t.exam).filter(x=>!/^\d{3}/.test(x)).slice(0,4)]);return dims.slice(0,6)}
function teacherIdeas(t){const topic=t.topic||'',ideas=[];if(/世界音樂|樂器|聲樂/.test(topic))ideas.push('🎧 先聽一小段代表聲響，不先公布名詞，請學生描述聽到的特徵。');else if(/曲式|樂理|和聲|織體|節奏/.test(topic))ideas.push('🧩 把結構或關鍵元素做成卡片，讓學生先分類、排列或找出差異。');else ideas.push('🎧 先用代表作品或聲音片段導入，讓學生從聽覺線索猜概念。');if(/中世紀|文藝復興|巴洛克|古典|浪漫|20世紀/.test(topic))ideas.push('🕰️ 把它放回音樂史時間軸，再連到同時期的人物、作品與社會背景。');if(t.related)ideas.push(`🔗 線索串聯：${t.related}。請學生說明這些線索為什麼和本名詞有關。`);ideas.push('✍️ 最後撤掉提示，用「是什麼＋關鍵背景＋代表人物／作品」寫成 3～5 句考場答案。');return ideas}
function scoreZh(t,input){if(!input.trim())return 0;return zhAnswers(t).some(a=>contains(input,a)||contains(a,input))?100:35}
function scoreExplain(t,input,dims){if(!input.trim())return 0;const hits=dims.filter(([,ks])=>ks.some(k=>contains(input,k))).length;return Math.round(hits/Math.max(1,dims.length)*100)}
function scoreTeaching(input){const s=String(input||'').trim();if(!s)return 0;let score=20;const groups=[/(學生|國中生|學習者|全班|小組)/,/(聽|看|唱|演奏|圖片|影片|音樂|作品|樂譜|卡片|聲音)/,/(問|猜|比較|分類|討論|發現|探索|辨識|排列|配對|遊戲|任務|挑戰)/,/(寫|說明|解釋|總結|反思|創作|設計|改編|出口票|驗收)/];groups.forEach(r=>{if(r.test(s))score+=20});if(s.length>=45)score+=10;return Math.min(100,score)}
function teachingFeedback(score){if(score>=90)return '已包含對象、素材、學生任務與學習產出，教學轉化很完整。';if(score>=70)return '已經有明確活動，若再補「學生最後要證明自己學會什麼」會更完整。';if(score>=50)return '有教學想法，但可以再加入學生實際要做的任務、比較或討論。';return '先不要只寫「老師講解」。試著加入聽、看、玩、想、說、寫或創作中的至少兩種學生行動。'}
function installStyles(){if($('termLearningStyles'))return;const s=document.createElement('style');s.id='termLearningStyles';s.textContent=`.term-recall{margin:16px 0}.term-recall textarea,.term-recall input{width:100%;border:1px solid var(--line);border-radius:12px;padding:12px;font:inherit;background:var(--card);color:var(--text)}.term-recall textarea{min-height:145px}.term-teaching-input{margin-top:16px;padding:14px;border:1px dashed var(--line);border-radius:14px;background:var(--soft)}.term-teaching-input textarea{min-height:110px;margin-top:8px}.term-confidence{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:8px 0 16px}.term-confidence button.active{outline:3px solid rgba(107,79,163,.28)}.term-diagnosis{display:grid;grid-template-columns:repeat(2,1fr);gap:9px;margin:12px 0}.term-diag{padding:12px;border:1px solid var(--line);border-radius:12px}.term-diag.hit{background:#eef7f1}.term-diag.miss{background:#fff3f1}.term-score{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:14px 0}.term-score>div{text-align:center;padding:12px;border:1px solid var(--line);border-radius:12px}.term-score strong{font-size:24px;display:block}.term-teacher{padding:14px;border-radius:14px;background:#fbf8ef;margin-top:14px}.term-teacher-feedback{padding:10px;border-radius:10px;background:var(--soft);margin:8px 0}.term-next-review{padding:12px;border-radius:12px;background:var(--soft);margin-top:12px;font-weight:700}.term-mastery{margin-top:10px;padding:12px;border-radius:12px;border:1px solid var(--line)}@media(max-width:620px){.term-confidence,.term-diagnosis,.term-score{grid-template-columns:1fr}}`;document.head.appendChild(s)}
function upgradeHomeEntry(){const btn=$('termBtn');if(!btn)return;btn.textContent='🧠 開始音樂名詞主動提取';const sec=btn.closest('.section-card');const h=sec?.querySelector('h3');const p=sec?.querySelector('p.muted');const n=window.MusicTeacherExam?.terms?.length||window.TERM_PROMPTS?.length||0;if(h)h.textContent='音樂名詞｜從會認到會寫、會教';if(p)p.textContent=`${n?`目前 ${n} 題｜`:''}練三條能力：🧠原文辨識、✍️教甄表達、👩‍🏫把專業轉成學生能參與的教學。`}
function setup(){upgradeHomeEntry();const view=$('termView');if(!view||view.dataset.termLearningReady)return;view.dataset.termLearningReady='1';installStyles();const zh=$('termZh'),show=$('showTermBtn');if(zh)zh.style.display='none';const old=show?.parentElement?.querySelector('.term-recall');if(old)old.remove();const box=document.createElement('div');box.className='term-recall';box.innerHTML=`<p class="muted">先不要看答案。看到原文後，測試自己能不能從腦中主動提取，再想想如果明天要教學生，你會怎麼讓他真的理解。</p><b>① 第一眼熟悉度</b><div class="term-confidence"><button class="ghost" data-c="2">🟢 我知道</button><button class="ghost" data-c="1">🟡 有印象</button><button class="ghost" data-c="0">🔴 完全不知道</button></div><b>② 中文名稱</b><input id="termZhRecall" placeholder="請自己輸入中文名稱"><br><br><b>③ 教甄名詞解釋</b><textarea id="termExplainRecall" placeholder="請用考場會寫的方式回答。可想：是什麼？何時？哪裡？誰？為什麼？如何？代表作品？"></textarea><div class="term-teaching-input"><b>④ 👩‍🏫 30 秒教學力挑戰</b><p class="muted">假設你明天要教國中生：你會用什麼聲音、圖片、問題、遊戲或任務，讓學生自己發現這個概念？</p><textarea id="termTeachRecall" placeholder="例如：先播放兩段音樂不公布答案，請學生小組比較……最後用一句話說出差異。"></textarea></div>`;show?.before(box);if(show)show.textContent='🔍 完成三項挑戰，開始診斷';let confidence=null;box.querySelectorAll('[data-c]').forEach(b=>b.onclick=()=>{confidence=Number(b.dataset.c);box.querySelectorAll('[data-c]').forEach(x=>x.classList.toggle('active',x===b))});show?.addEventListener('click',()=>setTimeout(()=>diagnose(confidence),0));$('nextTermBtn')?.addEventListener('click',()=>{confidence=null;setTimeout(reset,0)});$('termBackBtn')?.addEventListener('click',()=>{confidence=null;reset()})}
function reset(){['termZhRecall','termExplainRecall','termTeachRecall'].forEach(id=>{const x=$(id);if(x)x.value=''});document.querySelectorAll('.term-confidence button').forEach(x=>x.classList.remove('active'));const zh=$('termZh');if(zh)zh.style.display='none'}
function diagnose(confidence){const t=window.MusicTeacherExam?.terms?.find(x=>x.term===$('termName')?.textContent);if(!t)return;const zi=$('termZhRecall')?.value||'',ei=$('termExplainRecall')?.value||'',ti=$('termTeachRecall')?.value||'',dims=inferDims(t),rec=scoreZh(t,zi),exp=scoreExplain(t,ei,dims),teach=scoreTeaching(ti);const ans=$('termAnswer');if(!ans)return;const diag=dims.map(([label,ks])=>{const hit=ks.some(k=>contains(ei,k));return `<div class="term-diag ${hit?'hit':'miss'}"><b>${hit?'✓':'△'} ${esc(label)}</b><small>${hit?'你的答案有抓到這一塊':'這次可再補強'}${ks.length?`｜線索：${esc(ks.slice(0,3).join('、'))}`:''}</small></div>`}).join('');const ideas=teacherIdeas(t).map(x=>`<div>${esc(x)}</div>`).join('');const knowledge=Math.round(rec*.35+exp*.65),total=Math.round(rec*.25+exp*.5+teach*.25),data=load(),prev=data[t.id]||{},now=new Date(),wasDue=prev.due&&new Date(prev.due)<=now,success=rec===100&&knowledge>=80&&confidence!==0;let streak=Number(prev.spacedStreak||0),successfulReviews=Number(prev.successfulReviews||0);if(wasDue){if(success){streak+=1;successfulReviews+=1}else streak=0}const history=Array.isArray(prev.history)?prev.history.slice(-19):[];history.push({at:now.toISOString(),day:dayKey(now),score:total,knowledge,recognition:rec,expression:exp,teaching:teach,confidence,wasDue,success});const distinctSuccessDays=[...new Set(history.filter(x=>x.wasDue&&x.success).map(x=>x.day))].length;const mastery=successfulReviews>=3&&distinctSuccessDays>=3&&streak>=3&&knowledge>=80;let days;if(!success)days=1;else if(!wasDue)days=1;else days=intervals[Math.min(streak,intervals.length-1)];const d=new Date(now);d.setDate(d.getDate()+days);data[t.id]={...prev,recognition:rec,expression:exp,teaching:teach,score:total,knowledgeScore:knowledge,confidence,at:now.getTime(),due:d.toISOString(),spacedStreak:streak,successfulReviews,distinctSuccessDays,mastery,history};save(data);const masteryText=mastery?'🏆 知識真正掌握：已跨至少 3 個不同日期完成到期回想。':`🌱 知識尚未真正掌握：到期回想成功 ${successfulReviews} 次／至少需要 3 次；連續成功 ${streak} 次。`;ans.innerHTML=`<div class="feedback-title">📖 ${esc(t.zh||'')}</div><p>${esc(t.definition||'')}</p><div class="feedback-title">🧭 人事時地物 × 得分點診斷</div><div class="term-diagnosis">${diag}</div><div class="feedback-title">🎯 教甄答題重點</div><p>${esc(t.exam||'')}</p><div class="source-box">相關：${esc(t.related||'')}</div><div class="term-score"><div>🧠 辨識力<strong>${rec}</strong><small>/100</small></div><div>✍️ 表達力<strong>${exp}</strong><small>/100</small></div><div>👩‍🏫 教學力<strong>${teach}</strong><small>/100</small></div></div><div class="term-teacher-feedback"><b>👩‍🏫 你的教學力診斷</b><br>${esc(teachingFeedback(teach))}</div><div class="term-mastery"><b>${masteryText}</b><br><small>三條能力分開成長；「真正掌握」仍以跨天無提示提取為核心，不會因為教案寫得漂亮就取代知識熟練。</small></div><div class="term-teacher"><b>💡 教學靈感｜看完再比較自己的設計</b>${ideas}</div><div class="term-next-review">📊 本題三力綜合 ${total} 分｜知識熟練 ${knowledge} 分｜下一次安排 ${days} 天後（${d.getMonth()+1}/${d.getDate()}）無提示回想。</div>`;ans.classList.remove('hidden')}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(setup,50));else setTimeout(setup,50);
})();